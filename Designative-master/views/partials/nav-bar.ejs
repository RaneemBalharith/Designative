<div id="row0" class="row header">
  <nav class="navbar navbar-expand-lg">
    <!-- logo and brand name -->
    <a class="navbar-brand" href="/"> <span>Design</span><span>ative</span> </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- menu items and links -->
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link <%= path === '/' ? 'active' : '' %>" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/about' ? 'active' : '' %>" href="/about"
            >About Us</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/contact' ? 'active' : '' %>" href="/contact"
            >Contact Us</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link <%= path === '/faq' ? 'active' : '' %>" href="/faq">FAQ</a>
        </li>
      </ul>
    </div>
    <% if (!isAuthenticated) { %>
    <div
      class="navbar-nav ms-auto d-flex justify-content-end align-items-center signup-login">
      <a class="nav-item nav-link login" href="/login">Login</a>
      <a class="nav-item nav-link signup" href="/signup">SignUp</a>
    </div>
    <% } else {%>
    <div
      class="navbar-nav ms-auto d-flex justify-content-end align-items-center signup-login">
      <a class="nav-item nav-link signup" href="/logout">Logout</a>
    </div>
    <% } %>
  </nav>
</div>
